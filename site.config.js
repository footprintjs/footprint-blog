// Deploy config. The blog is published into the org Pages repo at footprintjs.github.io/blog,
// so it's served under this base path. Keep in sync with next.config.mjs basePath.
export const BASE = '/blog';
export const SITE = 'https://footprintjs.github.io';
